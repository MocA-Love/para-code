# PARA-CODE: fork-owned config (Para Code) — not present in upstream microsoft/vscode. See CLAUDE.md.
# SENTRY_AUTH_TOKEN is provided only through the build environment.

defaults.url=https://sentry.io/
defaults.org=maguro-bot-corp
defaults.project=para-code-mobile
